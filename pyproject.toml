[project]
name = "repl-nix-workspace"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.12"
dependencies = [
    "mpmath>=1.4.1",
    "numpy>=2.4.6",
    "plotly>=6.7.0",
]
